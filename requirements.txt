# gacha_vision only -- kept separate from the repo's TikTok pipeline deps.
# Also needs the Tesseract binary:  apt install tesseract-ocr
numpy>=1.24
opencv-python-headless>=4.8
pytesseract>=0.3.10
Pillow>=10.0
pytest>=8.0
